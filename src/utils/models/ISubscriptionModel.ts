import type { ReactNode } from "react";

export interface ISubscriptionModel {
	value: string;
	title: string;
	subtitle1: string;
	subtitle2: string;
	description: string;
	description2: string;
	image: ReactNode;
}
